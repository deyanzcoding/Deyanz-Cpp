{"version": "2.0.0",
"tasks": [
    {
        "label" : "build",
        "type" : "shall",
        "command" : "g++",
        "args" : [
        "-g",
        "\"${file}\"",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"],
        "options": {
        "cwd" : "${fileDirname}"
    }
    }
  ]
}