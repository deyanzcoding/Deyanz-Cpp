// program in less lines

#include<iostream>
using namespace std; int main(){int a,b,sum;cout<< "Enter 1st number : " << endl; cin >> a; cout<<"Enter 2nd number : " << endl; cin >> b; sum = a + b;  cout << "After sum of two numbers : " << sum; ;return 0;}


/* We can write our code in two lines, 
	1st line : for pre_processor directives
	2nd line : for any of ur code
	
	NOTE: pre_processor directive always in isolated line.
	 */